# addTwo

一个极简R包，提供两个函数：`addTwo(x, y)` 返回两数之和，`minusTwo(x, y)` 返回两数之差。

## 安装

你可以直接从GitHub安装（需要 R 已安装 `remotes` 包）：

```r
install.packages("remotes")
remotes::install_github("Shipeng-Guo/addTwo")
```

## 使用示例

```r
library(addTwo)
addTwo(1, 2)     # 3
minusTwo(5, 3)   # 2
```

